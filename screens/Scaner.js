import { View, Text } from "react-native"

export default function ScanerScreen() {
    return (
        <View style={{flex:1, alignItems: 'center', justifyContent: 'center'}}>
            <Text>ScanerScreen kraaaaas</Text>
        </View>
    )
}